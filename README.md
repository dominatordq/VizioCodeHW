# VizioCodeHW

Important Notes:

-Execute these npm commands before compiling/running:
  - npm install
  - npm install fetch
  - npm install jquery
  - npm install jsdom
  - npm install nodemon
  
To start, type 'npm start' in the terminal.
